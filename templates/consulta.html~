{% block body %}
 <form action="{{ url_for('consulta') }}" method=POST class=consulta>
   <dl>
     <dt>Descripcion:
     <dd><textarea name=descripcion rows=5 cols=40></textarea>
     <dd><input type=submit value=Enviar>
   </dl>
 </form>
 <ul class=consultas>
 {% for consulta in consultas %}
   <li><h1>{{ consulta.descripcion|safe }}</h1>
 {% else %}
   <li><em>Unbelievable.  No consultas here so far</em>
 {% endfor %}
 </ul>
 {% for message in get_flashed_messages() %}
   <div class=flash>{{ message }}</div>
 {% endfor %}
{% endblock %}
